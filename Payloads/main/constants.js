/**
 * constants.js
 * Shared configurables for the Electron browser shell.
 */
const DEFAULT_HOME = 'about:blank'; // ensure blank canvas until user navigates
const TOP_BAR_HEIGHT = 200; // reserved pixels for the custom chrome

module.exports = {
  DEFAULT_HOME,
  TOP_BAR_HEIGHT
};
